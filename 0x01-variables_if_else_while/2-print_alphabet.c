#include <stdio.h>

/**
 * main - entry point
 * description: print the alphabet lowercase
 * return: 0 (success)
*/

int main(void)
{
	char l;

	l = 'a';
	while
		(l <= 'z')
	{
		putchar(l);
		l++;
	}
	putchar('\n');
	return (0);
}

#include <stdio.h>

/**
 * main - print all aphapet letters in lower case
 *
 * Return: 0 (success)
*/

int main(void)
{
	char ch = 'a';

	while (ch = 'a')
	{
		putchar(ch)
		ch++;
	}
	putchar(\n);

	return (0);
}
